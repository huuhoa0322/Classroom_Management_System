/** Trang đăng nhập — sẽ được hoàn thiện ở P_FE5 (Auth feature) */
export default function LoginPage() {
  return (
    <div className="text-center py-4">
      <h2 className="text-xl font-semibold text-gray-700">Đăng nhập</h2>
      <p className="text-sm text-gray-400 mt-2">Trang này sẽ được hoàn thiện ở bước Auth Feature.</p>
    </div>
  );
}
